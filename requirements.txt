aiohttp>=3.10.0
anthropic>=0.40.0
redis[asyncio]>=5.0.0
asyncpg>=0.29.0
sqlalchemy[asyncio]>=2.0.0
httpx>=0.27.0
pydantic-settings>=2.0.0
scipy>=1.13.0
numpy>=1.26.4
python-telegram-bot>=21.0.0
fastapi>=0.115.0
uvicorn>=0.30.0
feedparser>=6.0.0
tweepy>=4.14.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
gspread>=6.0.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
pyyaml>=6.0
pandas>=2.0.0
shap>=0.45.0

# #CI-PYTHON-DEPS-0801 — dipendenze di RUNTIME che il codice importa da sempre e
# che non erano dichiarate. Scoperte dal primo giro del job Python in CI: in
# locale c'erano nella venv (installate a mano tempo addietro), quindi la suite
# passava e nessuno se ne accorgeva. Un `pip install -r requirements.txt` pulito
# non riusciva a importare il modello xG ne' il client Matchbook.
scikit-learn>=1.5.0   # models/calibration.py, models/xg_model.py
requests>=2.32.0      # core/matchbook_client.py

