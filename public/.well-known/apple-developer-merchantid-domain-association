# PLACEHOLDER — NOT a real Apple Pay merchant domain association file.
#
# This file MUST be replaced with the real domain association content
# generated by PayPal for the live merchant domain (betredge.com) before
# Apple Pay can validate this domain. Steps (Task 9):
#   1. Register the domain in the PayPal Dashboard > Apple Pay settings.
#   2. Download the merchant domain association file PayPal provides.
#   3. Replace this entire file's contents with that file's exact contents
#      (it is NOT plain text you can hand-author — do not edit its bytes).
#   4. Verify it is served at:
#      https://betredge.com/.well-known/apple-developer-merchantid-domain-association
#      (Next.js serves anything in /public at the site root — no route
#      config needed, this path resolves automatically).
#
# Until replaced, Apple Pay domain verification will FAIL and the Apple
# Pay button, even if rendered, will not be able to complete a real
# payment session in production.
PLACEHOLDER_REPLACE_WITH_REAL_PAYPAL_MERCHANT_DOMAIN_ASSOCIATION_FILE
