# SP4 — Risultati + risultati fine-partita sulla card · Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** (A) Mostrare il **risultato di fine partita** sulla card/scheda del feed quando un pick è settled (punteggio finale + esito ✓/✗/void, in linguaggio casual). (B) Creare la destinazione **`/risultati`** (track record + leaderboard come prova di fiducia), che sblocca anche il 404 della BottomNav.

**Architecture:** Strangler. Feed `/oggi` intatto; SP4 estende `PickCardVM`/`PickCard`/`PickCardExpanded` con lo stato settled, aggiunge `features/results/` (hook + ResultsScreen + TrackRecordView + LeaderboardView) che **consumano gli endpoint esistenti** (`/api/v2/history`, `/api/leaderboard`), e fa **una sola aggiunta backend additiva**: attaccare `final_score` a `/api/v2/predictions` (oggi c'è solo in `/api/v2/history`).

**Tech Stack:** Next.js 16, React 19, TS, Tailwind 4, Vitest + Testing Library.

**Fasi:** A (task 1–4) = risultati sulla card — l'ask esplicito di Andrea, priorità. B (task 5–8) = destinazione `/risultati`.

## Global Constraints

- **Prerequisito:** SP0–SP2 su `main` (SP3 in PR #145, non richiesto da SP4). Branch `feat/sp4-results` da `main`.
- **Dati reali (verificati 2026-07-12):**
  - `UnifiedPrediction.result ∈ {"won","lost","void","unresolved",null}` — trattare `"unresolved"` come `null`/pending (mai esito confermato). `status ∈ {"upcoming","open","pending_settlement","settled"}`. `result`/`status`/`settled_at` sono **PUBLIC_FIELDS** (visibili anche ad anonimo su card locked).
  - **Punteggio finale**: JSON in `notes` → `{"final_score":"2-1"}` (calcio) / `"6-4 6-3"` (tennis). Estrazione da portare: vedi `app/api/v2/history/route.ts:135-140`.
  - `/api/v2/predictions` serve il pick fino a **150 min** dopo `starts_at`, poi `is_historical=TRUE` lo sposta in `/api/v2/history`.
  - `/api/v2/history` → `{ history:[{...projected, final_score}], stats:{total,won,lost,void,pending,win_rate:"NN.N%"|null}, segments? }`. **Solo hit-rate, nessun ROI.**
  - `/api/leaderboard` GET → `{ leaderboard:[{rank,name,points,bets_won,bets_total,hit_rate,sport}], system_wins, system_hit_rate, system_settled, points_per_win:10 }`. Guard `MIN_SYSTEM_SETTLED=30` (system stats null sotto soglia).
- **FTC (vincolante):** MAI ROI/pnl/profit/stake (le colonne `leaderboard.pnl`/`bets.profit_loss` esistono ma NON vanno serializzate — non introdurle). Esito = "corretto/non riuscito", hit-rate; mai money. Mantenere il guard `MIN_SYSTEM_SETTLED`. Il pick `roi/market_roi/clv` in `useYearData.ts` sono placeholder non popolati — lasciarli fuori.
- **Gating:** `result`/`status`/`final_score` pubblici; il `pick` (quale selezione) resta gated. history/leaderboard non negano mai (degradano a locked). Non cambiare `showcaseAllowance`.
- **Scelte dichiarate (Andrea può correggere con `router`):**
  1. Card settled **locked**: mostra punteggio finale (pubblico) + badge esito ✓/✗ (rivela che il pick nascosto ha vinto/perso, **non** quale) come prova + upsell "sblocca per vedere il pronostico". Onesto e converte.
  2. `/risultati` = componenti **casual nuovi** in `features/results/` che consumano gli endpoint esistenti (non porto i componenti monolite `TrackRecordView`/`LeaderboardTab`; verranno dismessi in SP7).
  3. Backend: **una** aggiunta additiva (final_score in predictions route), nessun'altra modifica API.
- **Zero AI-slop, token `--am-coral*` (mai `--am-green`), TDD, commit frequenti, PR a fine SP.**

---

## FASE A — Risultati fine-partita sulla card

### Task 1: Backend — attaccare `final_score` a `/api/v2/predictions`

**Files:**
- Modify: `app/api/v2/predictions/route.ts`
- Test: `app/api/v2/predictions/final-score.test.ts` (unit sull'helper estratto)

**Interfaces:**
- Produces (estratto testabile): `parseFinalScore(notes: string | null): string | null` — estrae `final_score` dal JSON `notes`, `null` se assente/malformato.
- La response `predictions[]` avrà un campo `final_score: string | null` (come già fa `/api/v2/history`).

- [ ] **Step 1: Test che fallisce** — `app/api/v2/predictions/final-score.test.ts`

```ts
import { describe, it, expect } from "vitest";
import { parseFinalScore } from "./final-score";

describe("parseFinalScore", () => {
  it("estrae final_score da notes JSON", () => {
    expect(parseFinalScore('{"final_score":"2-1"}')).toBe("2-1");
    expect(parseFinalScore('{"final_score":"6-4 6-3","x":1}')).toBe("6-4 6-3");
  });
  it("null se assente/malformato/vuoto", () => {
    expect(parseFinalScore(null)).toBeNull();
    expect(parseFinalScore("")).toBeNull();
    expect(parseFinalScore("{}")).toBeNull();
    expect(parseFinalScore("not json")).toBeNull();
    expect(parseFinalScore('{"final_score":123}')).toBeNull();
  });
});
```

- [ ] **Step 2: RED** — `npx vitest run app/api/v2/predictions/final-score.test.ts` → FAIL.

- [ ] **Step 3: Implementare `app/api/v2/predictions/final-score.ts`**

```ts
export function parseFinalScore(notes: string | null): string | null {
  if (!notes) return null;
  try {
    const parsed = JSON.parse(notes);
    return typeof parsed?.final_score === "string" ? parsed.final_score : null;
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: Cablare in `app/api/v2/predictions/route.ts`** — dove costruisce `predictions = served.map((row) => {...})` (attorno a riga 184-188), attaccare `final_score: parseFinalScore((row as { notes?: string | null }).notes ?? null)` all'oggetto proiettato restituito (come `/api/v2/history:135-140`). Non cambiare la proiezione/gating.

- [ ] **Step 5: GREEN + verifica** — `npx vitest run app/api/v2/predictions/final-score.test.ts` → PASS; `npx tsc --noEmit` pulito.

- [ ] **Step 6: Commit** — `feat(sp4): attach final_score to /api/v2/predictions (parseFinalScore)`

---

### Task 2: View-model — stato settled su `PickCardVM`

**Files:**
- Modify: `features/feed/pick-view-model.ts`
- Modify: `features/feed/pick-view-model.test.ts`

**Interfaces:**
- Produces: `PickCardVM` con campi aggiuntivi `result: "won" | "lost" | "void" | null`, `finalScore: string | null`, `settled: boolean`. Helper `pickOutcomeLabel(result): string | null` ("Pronostico corretto" | "Non riuscito" | "Annullato" | null).
- `toPickCardVM`: `result` = normalizza `p.result` (`"unresolved"`→`null`, altrimenti il valore); `finalScore = p.final_score ?? null`; `settled = result !== null`.

- [ ] **Step 1: Test** (aggiungere): won→settled+label "Pronostico corretto"; lost→"Non riuscito"; void→"Annullato"; unresolved→result null, settled false; finalScore mappato da final_score; nessun result→settled false.
- [ ] **Step 2-4: RED → implementare (aggiungi i 3 campi al type + mapping in toPickCardVM + `pickOutcomeLabel`) → GREEN** (`npx vitest run features/feed/pick-view-model.test.ts`).
- [ ] **Step 5: Commit** — `feat(sp4): PickCardVM settled state (result/finalScore/settled + pickOutcomeLabel)`

---

### Task 3: `PickCard` — stato settled (punteggio + esito)

**Files:**
- Modify: `features/feed/PickCard.tsx`
- Modify: `features/feed/PickCard.test.tsx`

**Interfaces:**
- Quando `pick.settled`: la card mostra un **blocco risultato** — punteggio finale (`pick.finalScore`, se presente) + badge esito da `pickOutcomeLabel(pick.result)` con colore (won→verde `--am-coral`, lost→rosso `--am-negative`, void→muted). Sostituisce il `ConfidenceMeter` (la sicurezza pre-match non serve più) e il CTA "Perché" resta.
  - **Non-locked settled**: mostra decisione + punteggio + esito.
  - **Locked settled**: mostra match + punteggio finale (pubblico) + badge esito ("Il nostro pronostico: ✓ corretto / ✗ non riuscito", **senza** rivelare il pick) + "Prova Pro" (proof-driven upsell).
- FTC: solo esito/punteggio, nessun money.

- [ ] **Step 1: Test** — pick con `settled:true, result:"won", finalScore:"2-1"` (non-locked) → mostra "2-1" e "Pronostico corretto", niente ConfidenceMeter; `result:"lost"` → "Non riuscito"; locked+settled → mostra punteggio + esito ma NON la decisione in chiaro; pick non-settled → comportamento SP1/SP3 invariato (ConfidenceMeter presente).
- [ ] **Step 2-4: RED → implementare → GREEN.** Mantieni intatto lo stato non-settled (SP1/SP3) e `onOpen`/`onUpgrade`.
- [ ] **Step 5: Commit** — `feat(sp4): PickCard stato settled (punteggio finale + esito ✓/✗, locked→proof+upsell)`

---

### Task 4: `PickCardExpanded` — header risultato quando settled

**Files:**
- Modify: `features/feed/PickCardExpanded.tsx`
- Modify: `features/feed/PickCardExpanded.test.tsx`

**Interfaces:**
- Quando `pick.settled`: nel recap header mostra punteggio finale + badge esito (stesso `pickOutcomeLabel`), al posto del `ConfidenceMeter`. Il resto della scheda (mercati) resta invariato.

- [ ] **Step 1: Test** — pick settled won + finalScore → header mostra punteggio + "Pronostico corretto"; pick non-settled → ConfidenceMeter come prima (SP2).
- [ ] **Step 2-4: RED → implementare → GREEN.**
- [ ] **Step 5: Commit** — `feat(sp4): PickCardExpanded header risultato quando settled`

---

## FASE B — Destinazione `/risultati`

### Task 5: Hook `useTrackRecord` + `useLeaderboard`

**Files:**
- Create: `features/results/use-track-record.ts`
- Create: `features/results/use-leaderboard.ts`
- Test: `features/results/use-track-record.test.ts`, `features/results/use-leaderboard.test.ts`

**Interfaces:**
- `useTrackRecord(opts?: { sport?: string }): { history: TrackRow[]; stats: TrackStats | null; loading: boolean; error: string | null }` — fetch `GET /api/v2/history` (credentials include). `TrackStats = { total, won, lost, void, pending, winRate: string | null }` (mappa `win_rate`). `TrackRow` = `{ id, sport, competition, homeTeam, awayTeam, kickoff, finalScore, result, locked, decision? }` (mappa i campi proiettati; `decision` presente solo se non locked).
- `useLeaderboard(): { entries: LbRow[]; systemHitRate: string | null; systemSettled: number; pointsPerWin: number; loading; error }` — fetch `GET /api/leaderboard`. `LbRow = { rank, name, points, betsWon, betsTotal, hitRate, sport }`. **Non mappare mai pnl/roi.**

- [ ] **Step 1-5 (per ciascun hook):** test con fetch mockato (carica+mappa, errore→stato pulito), RED→implementa→GREEN. Commit unico: `feat(sp4): useTrackRecord + useLeaderboard (consumano history/leaderboard, no money)`.

---

### Task 6: `TrackRecordView` (casual)

**Files:**
- Create: `features/results/TrackRecordView.tsx`
- Test: `features/results/TrackRecordView.test.tsx`

**Interfaces:**
- `TrackRecordView()` usa `useTrackRecord`. Rende: **hero hit-rate** (`stats.winRate` + "X pick conclusi · Y corretti"), lista dei **pick conclusi recenti** (match, punteggio finale, esito ✓/✗, decisione se sbloccata altrimenti 🔒), stati loading/empty. Copy "prova di fiducia" (es. "Come sono andati i nostri pronostici — risultati reali, niente cherry-picking"). Nessun ROI. Se `winRate` null (pochi dati) → messaggio neutro, no percentuale inventata.

- [ ] **Step 1: Test** — con stats {total:10,won:7,winRate:"70.0%"} → mostra "70.0%" + conteggi; una riga conclusa mostra punteggio + esito; riga locked mostra 🔒 non la decisione; loading/empty gestiti.
- [ ] **Step 2-4: RED → implementare → GREEN.**
- [ ] **Step 5: Commit** — `feat(sp4): TrackRecordView casual (hit-rate + pick conclusi, no money)`

---

### Task 7: `LeaderboardView` (casual)

**Files:**
- Create: `features/results/LeaderboardView.tsx`
- Test: `features/results/LeaderboardView.test.tsx`

**Interfaces:**
- `LeaderboardView()` usa `useLeaderboard`. Rende: classifica (rank, nome, punti, hit-rate), + strip "system" con `systemHitRate` **solo se** `systemSettled >= 30` (altrimenti nascondi/neutro — rispetta il guard). Podio top-3 con marcatore testuale (no emoji-slop → usare un badge SVG/numero). Nessun money.

- [ ] **Step 1: Test** — entries rese con rank/nome/punti/hit-rate; systemHitRate mostrato se settled≥30, nascosto se <30; nessun pnl.
- [ ] **Step 2-4: RED → implementare → GREEN.**
- [ ] **Step 5: Commit** — `feat(sp4): LeaderboardView casual (+ guard system hit-rate <30)`

---

### Task 8: `ResultsScreen` + rotta `/risultati` + verifica

**Files:**
- Create: `features/results/ResultsScreen.tsx`
- Create: `app/risultati/page.tsx`
- Test: `features/results/ResultsScreen.test.tsx`

**Interfaces:**
- `ResultsScreen()` — compone header + `TrackRecordView` + `LeaderboardView` (tab o sezioni), footer disclaimer, `<BottomNav active="risultati" />`. 
- `app/risultati/page.tsx` — Server Component gated come `/oggi`: `notFound()` se `process.env.NEXT_PUBLIC_UX_NEW !== "1"`, altrimenti `<ResultsScreen/>`. Sblocca la voce BottomNav "Risultati" (oggi 404).

- [ ] **Step 1: Test** — ResultsScreen rende Track + Leaderboard + BottomNav con "Risultati" attivo (mock dei due hook).
- [ ] **Step 2-4: RED → implementare.** Poi `npm test && npx tsc --noEmit && npm run build`.
- [ ] **Step 5: Visual check** — flag on, `/risultati` rende track record + leaderboard; `/oggi` una card settled mostra punteggio+esito (verificare con un match concluso reale o via stato). Da loggato per il caso non-locked; anonimo per il locked+proof. Monolite intatto (`git status app/app/page.tsx`).
- [ ] **Step 6: Commit** — `feat(sp4): ResultsScreen + rotta /risultati (sblocca BottomNav)`

---

## Self-Review
- **Copertura:** risultati sulla card (ask Andrea) = Task 1–4; destinazione Risultati (spec IA §2) = Task 5–8; sblocca 404 BottomNav = Task 8. ✔
- **FTC:** solo hit-rate/esito/punteggio; mai ROI/pnl (mai mappati negli hook); guard MIN_SYSTEM_SETTLED preservato. ✔
- **Dati reali:** literal result/status, final_score da notes (port da history), endpoint esistenti consumati, gating pubblico su result/score. ✔
- **Placeholder:** Task 1-2 codice concreto; 3-8 interfacce + contratto di test (pattern componenti consolidato SP1-SP3). L'implementer si ferma se ambiguo.

## Rischi / questioni aperte
1. **final_score su predictions** (Task 1): additivo, basso rischio; nessun cambio di gating.
2. **Score tennis** ("6-4 6-3") vs calcio ("2-1"): mostrare la stringa così com'è (nessun parsing per-sport in SP4).
3. **Card settled locked** (scelta dichiarata): mostra esito ma non il pick — se Andrea preferisce nascondere anche l'esito ai locked, è un cambio a Task 3.
4. **Reuse vs rebuild** (scelta dichiarata): componenti casual nuovi che consumano gli endpoint; i componenti monolite si dismettono in SP7.

## Prossimo passo
Gate: codice prodotto + 1 modifica API = medium → **APPROVE SP4** prima di eseguire (subagent-driven). Backend Task 1 tocca un route servito ma è additivo (nessun cambio gating/dati).
