# Referral V2 Implementation Plan — Fase B (#REFERRAL-V2-0808)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

> 🔴 **GATE — QUESTO PIANO NON SI ESEGUE SENZA `APPROVE`.**
> Tocca `profiles` in produzione con utenti reali e **concede piani a pagamento gratis**. È medium/high per definizione. Questo documento è anche la **change-spec della PROPOSAL**: si posta, si aspetta l'OK di Andrea, e solo dopo si scrive codice.
> La migration 015 si applica **a mano nel SQL Editor di Supabase** (come 006/012/013): non esiste un runner. È un passo umano, irreversibile in avanti.

**Goal:** Trasformare il pannello *Invita* da attribuzione creator senza premio a un programma con una scala di premi ad accesso che scatta da sola, senza rompere l'attribuzione esistente né i codici già distribuiti.

**Architecture:** Si aggiunge sopra l'esistente. `profiles.referral_code` / `referred_by` / `/api/referral/claim` restano intatti. Un helper nuovo (`lib/referral-rewards.ts`) è l'unico posto che sa contare gli invitati paganti e concedere i premi; i rail di pagamento lo chiamano dopo ogni grant riuscito. I premi sono giorni di piano e un flag, mai denaro.

**Tech Stack:** Next.js (⚠️ vedi Global Constraints), TypeScript, Postgres via `dbQuery`/`dbExecute`/`dbQueryStrict` (Supabase), Vitest.

## Global Constraints

- **Questo NON è il Next.js che conosci.** Leggere `node_modules/next/dist/docs/` prima di toccare route handlers (istruzione di `AGENTS.md`).
- **Branch:** `betredge/tools-11-referral-v2`. Mai commit su main, mai push ravvicinati. Al commit aggiungere **solo i propri file, elencati** — mai `git add -A`.
- **`exec_sql` NON restituisce `RETURNING`.** Pattern obbligatorio: INSERT (o UPDATE guardato), poi SELECT di verifica. Già usato in `/api/referral/claim` e in `plan-grant.ts` — copiare da lì.
- **I premi sono ACCESSO, mai denaro.** Nessun credito, nessun payout, nessuno sconto in valuta. È il vincolo che tiene il programma dentro la VIA A ([[project_gambling_qualification]], rischio #1 del go-live). Se un task sembra richiedere denaro, fermarsi.
- **Un grant pagato non deve MAI fallire per colpa del referral.** Ogni chiamata al codice dei premi è in `try/catch` con log, come già fa `sendPlanReceipt`. Un premio mancato si recupera a mano; un pagamento non concesso è un cliente perso.
- **Il conteggio è monotono.** Si contano i pagamenti *avvenuti* (ordini con `granted_at IS NOT NULL`), non gli abbonati attivi. Un amico che paga e poi disdice resta contato per sempre.
- **Non toccare** `lib/referral-code.ts`, `/api/referral/claim`, la logica di `computePaygateGrant`, né le colonne `creator_revenue_*` della 012.
- Prezzi di riferimento in `lib/commercial-plan.ts`: BASE **$14.99**, PRO **$29.99**. Non hardcodarli in nessun test — leggerli.

---

## File Structure

| File | Responsabilità | Azione |
|---|---|---|
| `db/migrations/015_referral_rewards.sql` | Tabella `referral_rewards` + flag del gradino 10 | **Create** (apply manuale in Supabase) |
| `lib/referral-rewards.ts` | **Unico** posto che conta gli invitati paganti, valuta i gradini e concede i premi | **Create** |
| `lib/referral-rewards.test.ts` | Test dell'helper, inclusa l'invariante anti-arbitraggio | **Create** |
| `lib/plan-grant.ts` | Aggancio ai 5 rail + il grant del premio con `plan_source='referral'` | **Modify** |
| `app/api/auth/route.ts` | Bonus 7 giorni all'invitato, **all'attivazione** | **Modify** |
| `app/api/referral/stats/route.ts` | `paying` al posto di `paid` + stato dei gradini | **Modify** |
| `app/app/page.tsx` (`ReferralPanel`, ~7429) | Progresso verso il gradino, cosa ottiene l'amico | **Modify** |

**Ordine:** la migration e l'helper prima (tutto il resto ne dipende), poi l'aggancio ai rail, poi il bonus invitato, poi l'API, poi la UI. Ogni task è committabile e testabile da solo.

---

### Task 1: Migration 015 — la tabella dei premi

**Files:**
- Create: `db/migrations/015_referral_rewards.sql`

**Interfaces:**
- Produces: tabella `referral_rewards`, colonna `profiles.referral_room_access`.

- [ ] **Step 1: Leggere la 013 per lo stile**

Run: `cat db/migrations/013_referral_code.sql`
Serve la forma reale dei commenti e delle guardie `IF NOT EXISTS` usate in questo repo, e la nota «Eseguire nel SQL Editor Supabase».

- [ ] **Step 2: Scrivere la migration**

```sql
-- 015_referral_rewards.sql — #REFERRAL-V2-0808
-- Eseguire nel SQL Editor Supabase (come 006/012/013).
--
-- La scala premi del referral. Un invito è VALIDO solo se l'invitato ha pagato
-- almeno una volta (scelta di Andrea 2026-08-08): il conteggio guarda gli ordini
-- con granted_at, non profiles.plan, così un amico che paga e poi disdice resta
-- contato e il premio non regredisce dopo essere stato concesso.
--
-- Soglie: 2 → 29gg PRO · 5 → altri 60gg · 10 → stanza Telegram riservata.
-- (29 e non 30 per l'invariante anti-arbitraggio: 2 × BASE = $29.98 di costo
--  minimo contro $29.99 che valevano 30 giorni di PRO. Deciso da Andrea.)
-- tier 0 = bonus dell'INVITATO (7 giorni), tenuto nella stessa tabella perché
-- ha la stessa esigenza: concedere una volta sola.
--
-- I premi sono ACCESSO, mai denaro (vincolo VIA A).

CREATE TABLE IF NOT EXISTS public.referral_rewards (
  id            BIGSERIAL PRIMARY KEY,
  identifier    TEXT        NOT NULL,
  tier          SMALLINT    NOT NULL,
  granted_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  paying_count  INT         NOT NULL,
  CONSTRAINT chk_referral_tier CHECK (tier IN (0, 2, 5, 10))
);

-- Il lock vero contro il doppio grant: due richieste concorrenti sullo stesso
-- gradino non possono vincere entrambe.
CREATE UNIQUE INDEX IF NOT EXISTS uq_referral_rewards_identifier_tier
  ON public.referral_rewards (identifier, tier);

CREATE INDEX IF NOT EXISTS idx_referral_rewards_identifier
  ON public.referral_rewards (identifier);

-- Gradino 10: accesso alla stanza riservata. Revocabile — lo legge il bot.
-- "Attivo" = piano a pagamento non scaduto, inclusi i giorni regalati.
ALTER TABLE public.profiles
  ADD COLUMN IF NOT EXISTS referral_room_access BOOLEAN NOT NULL DEFAULT FALSE;
```

- [ ] **Step 3: Verificare la sintassi SENZA applicarla**

Run: `grep -c "IF NOT EXISTS" db/migrations/015_referral_rewards.sql`
Expected: 4 (tabella, due indici, colonna) — tutto ri-eseguibile senza errore.
**NON applicare la migration.** L'apply è un passo umano nel gate.

- [ ] **Step 4: Commit**

```bash
git add db/migrations/015_referral_rewards.sql
git commit -m "feat(db): migration 015 — tabella referral_rewards + flag stanza (#REFERRAL-V2-0808)"
```

---

### Task 2: `lib/referral-rewards.ts` — il conteggio e la scala

**Files:**
- Create: `lib/referral-rewards.ts`
- Create: `lib/referral-rewards.test.ts`

**Interfaces:**
- Consumes: `dbQuery`, `dbQueryStrict`, `dbExecute` da `./db`; `computePaygateGrant` da `./plan-grant`; `COMMERCIAL_PLANS` (o l'export equivalente) da `./commercial-plan`.
- Produces:
  - `REFERRAL_TIERS: { tier: 2|5|10; rewardDays: number | null; grantsRoom: boolean }[]`
  - `INVITEE_BONUS_DAYS = 7`
  - `countPayingInvitees(code: string, selfIdentifier: string): Promise<number>`
  - `reachedTiers(payingCount: number): number[]`
  - `checkReferralTiers(invitedIdentifier: string): Promise<void>` — best-effort, non lancia mai
  - `grantInviteeBonus(identifier: string): Promise<boolean>`

- [ ] **Step 1: Scrivere i test puri (nessun DB)**

`reachedTiers` e l'invariante anti-arbitraggio sono pure: si testano senza mock.

```ts
import { describe, it, expect } from "vitest";
import { REFERRAL_TIERS, reachedTiers, INVITEE_BONUS_DAYS } from "./referral-rewards";
import { /* l'export dei prezzi */ } from "./commercial-plan";

describe("reachedTiers", () => {
  it("sotto la prima soglia non sblocca niente", () => {
    expect(reachedTiers(0)).toEqual([]);
    expect(reachedTiers(1)).toEqual([]);
  });
  it("a 2 sblocca il primo gradino", () => {
    expect(reachedTiers(2)).toEqual([2]);
  });
  it("a 10 li ha sbloccati tutti e tre", () => {
    expect(reachedTiers(10)).toEqual([2, 5, 10]);
  });
  it("oltre la soglia massima non inventa gradini", () => {
    expect(reachedTiers(50)).toEqual([2, 5, 10]);
  });
});

describe("invariante anti-arbitraggio", () => {
  // Per ogni gradino: il premio non deve valere più di quanto costa sbloccarlo,
  // altrimenti il programma regala prodotto invece di acquisire utenti.
  // I prezzi si LEGGONO da commercial-plan: se domani cambiano, questo test
  // diventa rosso invece di lasciare aperto un arbitraggio.
  it("nessun gradino è economicamente farmabile", () => {
    const proMonthly = /* prezzo PRO da commercial-plan */;
    const cheapestPaid = /* min(BASE, PRO) da commercial-plan */;
    for (const t of REFERRAL_TIERS) {
      if (t.rewardDays === null) continue; // la stanza non ha valore monetario
      const rewardValue = proMonthly * (t.rewardDays / 30);
      const attackCost = t.tier * cheapestPaid;
      expect(rewardValue).toBeLessThanOrEqual(attackCost);
    }
  });

  it("il bonus dell'invitato non supera il piano più economico", () => {
    const proMonthly = /* prezzo PRO */;
    const cheapestPaid = /* min */;
    expect(proMonthly * (INVITEE_BONUS_DAYS / 30)).toBeLessThanOrEqual(cheapestPaid);
  });
});
```

⚠️ **Prima di scrivere questi test, leggere `lib/commercial-plan.ts`** e usare i nomi di export reali. Se i prezzi non sono esposti in una forma leggibile da un test, esporli — ma **senza** cambiare i valori.

⚠️ **Se l'invariante è rossa, NON abbassarla.** Significa che una soglia o un premio è sbagliato: riportarlo ad Andrea.

**Già capitato e già risolto:** a 30 giorni il primo gradino valeva $29.99 contro $29.98 di costo minimo (2 × BASE) e l'invariante **fallìva di un centesimo**. Andrea ha deciso il **premio a 29 giorni** (2026-08-08): $28.99 contro $29.98, circa un euro di margine, e fra 29 e 30 giorni l'utente non percepisce differenza. Le soglie restano 2/5/10. Con questi valori il test deve passare **al primo giro**: se è rosso, qualcosa è stato scritto male, non è la realtà che è cambiata.

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/referral-rewards.test.ts`
Expected: FAIL — il modulo non esiste.

- [ ] **Step 3: Implementare l'helper**

```ts
// lib/referral-rewards.ts — #REFERRAL-V2-0808
// Unico posto che sa (a) chi conta come invitato pagante e (b) cosa si sblocca.
// Sta separato da plan-grant.ts perché quello owna il DENARO: qui si regala
// accesso, e i due non devono potersi confondere.

import { dbQuery, dbQueryStrict, dbExecute } from "./db";
import { computePaygateGrant } from "./plan-grant";

export const INVITEE_BONUS_DAYS = 7;

export const REFERRAL_TIERS = [
  // 29 e non 30: deciso da Andrea per rispettare l'invariante anti-arbitraggio
  // (a 30 il premio valeva $29.99 contro $29.98 di costo minimo — vedi step 1).
  { tier: 2 as const,  rewardDays: 29,   grantsRoom: false },
  { tier: 5 as const,  rewardDays: 60,   grantsRoom: false },
  { tier: 10 as const, rewardDays: null, grantsRoom: true  },
];

/** I gradini raggiunti con N invitati paganti, in ordine crescente. */
export function reachedTiers(payingCount: number): number[] {
  return REFERRAL_TIERS.filter((t) => payingCount >= t.tier).map((t) => t.tier);
}

/** Invitati col mio codice che hanno pagato ALMENO UNA VOLTA.
 *  Guarda gli ordini granted, non profiles.plan: un amico che paga e poi
 *  disdice resta contato ⇒ il conteggio non regredisce mai.
 *  ⚠️ Le tabelle ordini oggi sono due; con PR #217 arriva Shopify: questa
 *  query è l'UNICO posto da aggiornare. */
export async function countPayingInvitees(code: string, selfIdentifier: string): Promise<number> {
  const rows = await dbQueryStrict<{ n: number }>(
    `SELECT COUNT(DISTINCT p.identifier)::int AS n
       FROM profiles p
       JOIN (
         SELECT identifier FROM paygate_orders WHERE granted_at IS NOT NULL
         UNION ALL
         SELECT identifier FROM paypal_orders  WHERE granted_at IS NOT NULL
       ) o ON o.identifier = p.identifier
      WHERE UPPER(p.referred_by) = $1
        AND p.identifier <> $2`,
    [code.toUpperCase(), selfIdentifier]
  );
  return Number(rows[0]?.n ?? 0);
}
```

Il resto dell'helper — `claimTier` (INSERT + SELECT di verifica, mai `RETURNING`), il grant dei giorni **via `computePaygateGrant`** con `plan_source='referral'`, e `checkReferralTiers` che risale da `referred_by` all'invitante — segue i pattern già in `plan-grant.ts`. **Leggerlo e copiarne la forma**, non inventarne una nuova.

- [ ] **Step 4: Scrivere i test del grant (con DB mockato o su schema di test)**

Casi obbligatori — questi sono i bug che il design ha già evitato sulla carta e che il test deve inchiodare:
1. **Il grant estende, non accorcia:** utente con PRO attivo a 20 giorni + premio da 29 → **49 giorni**. (Se qualcuno riscrive `plan_expires_at = NOW() + INTERVAL '29 days'` questo test diventa rosso.)
2. **Idempotenza:** due `checkReferralTiers` concorrenti sullo stesso gradino → **un solo** grant (lo `UNIQUE` regge, il secondo prende il 409 e non concede).
3. **Monotonia:** un invitato che ha pagato e ora è `plan='free'` **è ancora contato**.
4. **Anti self-referral:** il proprio identifier è escluso dal conteggio.
5. `checkReferralTiers` su un utente **senza** `referred_by` non fa nulla e non lancia.
6. **Non lancia mai:** con il DB che throwa, `checkReferralTiers` risolve comunque (il chiamante è un rail di pagamento).

- [ ] **Step 5: Run tests**

Run: `npx vitest run lib/referral-rewards.test.ts && npx tsc --noEmit`
Expected: verde e pulito.

- [ ] **Step 6: Commit**

```bash
git add lib/referral-rewards.ts lib/referral-rewards.test.ts
git commit -m "feat(referral): helper del conteggio paganti e della scala premi (#REFERRAL-V2-0808)"
```

---

### Task 3: Agganciare i rail — il punto più delicato del piano

**Files:**
- Modify: `lib/plan-grant.ts`

**Interfaces:**
- Consumes: `checkReferralTiers` dal Task 2.

- [ ] **Step 1: Capire perché l'aggancio NON va nel ramo della transizione**

Leggere `activateStripePlan`, `activatePaygatePlan`, `activatePaypalPlan`, `activateShopifyPlan`, `activateAdminPlan`: tutte chiamano `notifyPlanActivated` **solo se `before.plan !== plan`**.

**Il caso che quel ramo perde:** l'invitato riceve i 7 giorni di PRO regalati (`plan='premium'`, `plan_source='referral'`), poi **paga** premium. `before.plan === plan` ⇒ nessuna transizione ⇒ nessuna notifica. Se il controllo dei gradini stesse lì, chi ha invitato **non riceverebbe mai credito per l'amico con la più alta probabilità di pagare** — proprio perché era dentro il prodotto grazie al regalo.

⇒ `checkReferralTiers` va chiamato **dopo l'UPDATE riuscito, fuori dall'`if` della transizione**, in tutte e cinque le funzioni.

- [ ] **Step 2: Scrivere il test che inchioda esattamente quel caso**

```ts
it("un invitato con PRO regalato che poi PAGA fa scattare il conteggio", async () => {
  // profilo: plan='premium', plan_source='referral', scadenza futura
  // → activateShopifyPlan(identifier, 'premium', 'monthly')
  // before.plan === 'premium' === plan ⇒ NESSUNA transizione
  // Atteso: checkReferralTiers chiamato COMUNQUE, una volta, con questo identifier.
  expect(checkReferralTiersSpy).toHaveBeenCalledWith(identifier);
});

it("se checkReferralTiers lancia, il grant riesce comunque", async () => {
  checkReferralTiersSpy.mockRejectedValue(new Error("boom"));
  const r = await activateShopifyPlan(identifier, "premium", "monthly");
  expect(r).not.toBeNull();   // i soldi sono arrivati: il piano si concede
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run lib/plan-grant.test.ts` (o il file di test reale — trovarlo con `ls lib/*plan-grant*`)
Expected: FAIL

- [ ] **Step 4: Implementare l'aggancio**

In ognuna delle cinque funzioni, dopo l'UPDATE e **fuori** dall'`if (before.plan !== plan)`:

```ts
  // #REFERRAL-V2-0808: fuori dal ramo della transizione di proposito — un
  // invitato che ha già PRO regalato e poi paga NON transiziona, ma è
  // esattamente l'amico che deve contare per chi lo ha invitato.
  // Best-effort: un premio mancato si recupera, un pagamento non concesso no.
  try {
    await checkReferralTiers(identifier);
  } catch (e) {
    console.error("[referral] check dei gradini fallito:", String(e));
  }
```

- [ ] **Step 5: Run tests**

Run: `npx vitest run && npx tsc --noEmit`
Expected: verde. **Tutti** i test esistenti dei rail di pagamento devono continuare a passare: questo file governa il denaro.

- [ ] **Step 6: Commit**

```bash
git add lib/plan-grant.ts lib/plan-grant.test.ts
git commit -m "feat(referral): controllo dei gradini su ogni grant riuscito, non sulla transizione (#REFERRAL-V2-0808)"
```

---

### Task 4: Il bonus dell'invitato, all'attivazione

**Files:**
- Modify: `app/api/auth/route.ts`

- [ ] **Step 1: Capire perché non va all'INSERT**

`app/api/auth/route.ts:366` inserisce il profilo con `plan='free'` e **l'account è usabile solo dopo il click sul link di attivazione** (`activated_at`, guardia HIGH-3). Concedere 7 giorni di PRO all'INSERT li farebbe **scadere prima che l'utente possa entrare**: il regalo diventerebbe un insulto.

Ci sono **due** percorsi di attivazione e servono entrambi:
1. `if (!emailActivationRequired())` — auto-attivazione inline (righe ~377-382)
2. La route del link email — trovarla: `grep -rn "activated_at = NOW()" app/api/`

- [ ] **Step 2: Scrivere i test**

```ts
it("all'attivazione, un profilo con referred_by riceve 7 giorni di PRO", async () => {
  // referred_by = 'AMICO', plan='free' → attivazione
  // Atteso: plan='premium', plan_source='referral', scadenza ≈ +7 giorni,
  //         riga in referral_rewards con tier=0
});

it("un profilo SENZA referred_by non riceve niente", async () => { /* plan resta 'free' */ });

it("il bonus si concede una volta sola (ri-attivazione non lo raddoppia)", async () => {
  // seconda attivazione → nessun secondo grant (UNIQUE su tier=0)
});

it("un codice che non esiste non concede il bonus", async () => {
  // referred_by = 'CODICEINVENTATO' → nessun premio: il codice va validato
  // contro profiles.referral_code, non solo contro la regex.
});
```

⚠️ L'ultimo caso è importante: `referred_by` alla registrazione passa **solo la regex** (`route.ts:351`), non un controllo di esistenza. Senza validazione, chiunque si registra con `?ref=QUALSIASICOSA` si prende 7 giorni di PRO gratis — **un bonus farmabile con un solo account per volta, ma farmabile all'infinito**.

- [ ] **Step 3: Run test to verify it fails** → FAIL

- [ ] **Step 4: Implementare `grantInviteeBonus` nei due percorsi**

Valida che `referred_by` corrisponda a un `profiles.referral_code` esistente, poi concede via `computePaygateGrant` (`days: 7`, `plan: 'premium'`, `plan_source: 'referral'`), registrando `tier = 0` in `referral_rewards`. In `try/catch`: un bonus mancato non deve impedire un'attivazione.

- [ ] **Step 5: Run tests** → `npx vitest run && npx tsc --noEmit` verde

- [ ] **Step 6: Commit**

```bash
git add app/api/auth/route.ts lib/referral-rewards.ts
git commit -m "feat(referral): 7 giorni di PRO all'invitato, concessi all'attivazione (#REFERRAL-V2-0808)"
```

---

### Task 5: `/api/referral/stats` — `paying` al posto di `paid`

**Files:**
- Modify: `app/api/referral/stats/route.ts`

- [ ] **Step 1: Verificare che nessun altro consumi `paid`**

Run: `grep -rn "referral/stats\|\.paid\b" app components lib | grep -v node_modules`
Se qualcosa oltre `ReferralPanel` legge `paid`, **fermarsi e riportarlo**: la spec assume un solo consumatore.

- [ ] **Step 2: Scrivere i test**

```ts
it("torna paying (pagamenti avvenuti), non gli abbonati attivi", async () => {
  // un invitato che ha pagato e ora è free ⇒ paying = 1
});
it("torna lo stato dei tre gradini", async () => {
  // { tiers: [{tier:2,reached:true,granted_at:...},{tier:5,reached:false,...},...] }
});
it("senza codice claimato resta 403 con la ragione", async () => { /* comportamento attuale, non regredire */ });
```

- [ ] **Step 3: Run test to verify it fails** → FAIL

- [ ] **Step 4: Implementare**

Risposta: `{ code, signups, paying, tiers }`. `signups` **resta** (non rompere la UI attuale). `paid` viene **rimosso** e sostituito da `paying`.

- [ ] **Step 5: Run tests** → verde

- [ ] **Step 6: Commit**

```bash
git add app/api/referral/stats/route.ts
git commit -m "feat(referral): stats espone i pagamenti avvenuti e lo stato dei gradini (#REFERRAL-V2-0808)"
```

---

### Task 6: Il pannello *Invita* mostra il progresso

**Files:**
- Modify: `app/app/page.tsx` — funzione `ReferralPanel` (~riga 7429)

- [ ] **Step 1: Leggere il pannello attuale**

Run: `sed -n '7429,7560p' app/app/page.tsx`
Il dizionario è inline con `pick5` (5 lingue: it/en/es/fr/ru — **non** 11 come i tool). Mantenere quella forma.

- [ ] **Step 2: Aggiungere al pannello**

1. **Progresso verso il gradino successivo** — «3 amici paganti · 2 al prossimo premio», coi tre gradini visibili e quelli raggiunti marcati. **È il motore del programma:** senza, l'utente non sa di essere a metà strada.
2. **Cosa ottiene l'amico** (i 7 giorni): è l'argomento che chi invita userà per convincerlo, quindi va detto a chi invita.
3. La nota sulla rev-share **resta ma scende** in tono minore: non è più l'offerta principale.
4. **Togliere la promessa vuota** «−50% sul primo acquisto» come se fosse un vantaggio del referral: è la promo di lancio che riceve chiunque. Dirlo come vantaggio dell'invito era il difetto originale.

- [ ] **Step 3: Verifica visiva DA LOGGATO**

Non basta il DOM: headless + cookie Chrome su un profilo reale con invitati paganti (regola standing: mai solo da anonimo). Controllare anche **mobile a 390px** con Playwright — l'estensione Chrome non cambia il viewport.
Expected: il progresso mostra i numeri veri dell'account, i gradini raggiunti sono marcati, nessun overflow orizzontale.

- [ ] **Step 4: Commit**

```bash
git add app/app/page.tsx
git commit -m "feat(referral): progresso verso i gradini nel pannello Invita (#REFERRAL-V2-0808)"
```

---

### Task 7: Verifica finale e PR

- [ ] **Step 1: Suite completa** → `npx vitest run && npx tsc --noEmit` verde
- [ ] **Step 2: Ripassare i 8 criteri della spec** (§Verifica, punti 8-15) uno per uno, con l'output vero
- [ ] **Step 3: PR** — nel corpo: che la **migration 015 va applicata a mano in Supabase prima del deploy**, e che l'invariante anti-arbitraggio è a un centesimo dal limite (§Task 2 step 1)
- [ ] **Step 4:** ⚠️ **Fermarsi alla PR.** Il deploy e l'apply della migration sono decisioni di Andrea.

---

## Self-Review

**1. Spec coverage.** §1.1 architettura → T1+T2 · §1.2 conteggio monotono → T2 (test 3) · §1.3 scala e grant via `computePaygateGrant` → T2 (test 1) · §1.3 idempotenza → T1 (UNIQUE) + T2 (test 2) · §1.3 definizione di «attivo» per il gradino 10 → T1 (colonna + commento); **la revoca vera quando il piano scade non è in questo piano** — appartiene al workstream Telegram che legge il flag, come dichiarato nel Fuori scope della spec · §1.4 bonus invitato → T4 · §1.5 anti-abuso → T2 (test 4) + T4 (validazione del codice) · §1.5.1 invariante → T2 step 1 · §1.6 UI → T6 · §1.7 aggancio ai rail → T3.

**2. Placeholder scan.** Restano tre punti dove il piano dice *leggi il codice e usa i nomi reali* invece di inventare una firma: gli export dei prezzi in `commercial-plan.ts` (T2 step 1), il file di test reale di `plan-grant` (T3 step 3), e la route del link di attivazione (T4 step 1). Sono deliberati: inventare quei nomi produrrebbe codice che non compila.

**3. Type consistency.** `reachedTiers(n): number[]` · `countPayingInvitees(code, self): Promise<number>` · `checkReferralTiers(id): Promise<void>` e non lancia · `REFERRAL_TIERS[].rewardDays: number | null` (`null` = la stanza, che non ha valore monetario — ed è per questo che l'invariante la salta). `tier` ∈ {0,2,5,10} sia nel CHECK della 015 sia in `REFERRAL_TIERS` ∪ {0 del bonus invitato}.

**Il punto che va discusso prima di scrivere codice** (T2 step 1): ai prezzi di oggi l'invariante anti-arbitraggio **fallisce di 1 centesimo** sul primo gradino — 2 × $14.99 = $29.98 contro $29.99 di premio. Le opzioni sono un premio a 29 giorni o una soglia a 3. Non la si aggira abbassando il test.
