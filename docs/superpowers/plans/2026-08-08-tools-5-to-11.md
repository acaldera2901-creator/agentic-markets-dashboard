# Tools 5→11 Implementation Plan — Fase A (#TOOLS-11-0808)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Portare l'hub `/tools` da 5 a 11 calcolatori (66 → 132 URL statiche in 11 lingue), riusando l'infrastruttura di `#TOOLS-HUB-0805` senza modificarla.

**Architecture:** Ogni tool è una fetta verticale: una funzione pura in `lib/betting-math.ts`, un componente client in `components/tools/`, una voce in `TOOL_SLUGS`, la copy in tutte e 11 le lingue, i test. Routing, SEO, hreflang e sitemap sono già generati da `TOOL_SLUGS`: aggiungere uno slug è l'unica azione infrastrutturale necessaria.

**Tech Stack:** Next.js (⚠️ vedi Global Constraints), TypeScript, React client components, Vitest + @testing-library/react, Playwright.

## Global Constraints

- **Questo NON è il Next.js che conosci.** Prima di scrivere codice di routing/pagine, leggere la guida in `node_modules/next/dist/docs/` (istruzione di `AGENTS.md`). Le API possono differire dal training.
- **Branch:** `betredge/tools-11-referral-v2`, base `c6f9f8c` (= `origin/main`). Mai commit su main, mai push ravvicinati.
- **`ToolsCopy.tools` è `Record<ToolSlug, ToolCopy>`** (`lib/tools/copy/types.ts:60`): appena uno slug entra in `TOOL_SLUGS`, **tutte e 11 le lingue devono avere quella chiave** o il progetto non compila. Per questo ogni task è *un tool completo in 11 lingue*, non "prima il codice poi le traduzioni".
- **Mai `NaN` né `Infinity` a schermo.** Valore non calcolabile → il trattino da `copy.common.invalid`, passato come prop `dash`. Vale per ogni readout, sempre.
- **Nessun blocco formula** nelle pagine (decisione Andrea 2026-08-05): al suo posto l'esempio numerico lavorato. I simboli matematici non li legge nessuno.
- **Non modificare** `ToolShell.tsx`, `parts.tsx`, `Prose.tsx`, `Meter.tsx`, `lib/tools/seo.ts`, `LocalePicker.tsx`. Se un tool sembra richiederlo, fermarsi e segnalarlo: significa che sta uscendo dal pattern.
- **I numeri degli esempi sono identici in ogni lingua**, si traducono solo le etichette (`types.ts:24`).
- **Soglia dell'explainer in CARATTERI, non parole** (>800): polacco e turco dicono lo stesso con 40 parole in meno ma pari caratteri.
- **Slug in inglese anche sulle lingue**; `en` vive senza prefisso su `/tools` (canonical).
- Percentuali in input come numeri interi: `55` significa 55%. `parsePercent` accetta `0 < n < 1` dopo la divisione.

### Correzioni imposte dal Task 1 (scoperte eseguendolo — valgono per tutti i task successivi)

- **RICALCOLA ogni numero degli esempi con `npx tsx` prima di scriverlo.** Le tabelle di questo piano hanno già contenuto un errore: `1.90/1.90` dà **−5.00%**, non −4.99% (`1/1.9 × 2 = 1.052632`, reciproco `0.95` esatto). Non fidarti delle tabelle: verificale. *(Bella simmetria da usare in copy: `2.10/2.10` = +5.00%, `1.90/1.90` = −5.00%.)*
- **Tre contatori hardcodati vanno alzati a ogni tool**, non uno. Nessuno di questi è generico su `TOOL_SLUGS`:
  - `lib/tools/copy.test.ts` → `expect(TOOL_SLUGS).toHaveLength(N)`
  - `lib/tools/seo.test.ts` → conteggio dei tool
  - `app/sitemap.test.ts` → conteggio delle URL (77 dopo il Task 1, +11 per tool)
- **Nomi CSS reali** (il JSX del Task 1 ne sbagliava tre): il bottone è **`tl-btn`** (non `tl-add`); **`tl-verdict.is-ok` NON esiste** — in `globals.css` ci sono solo `tl-verdict` e `.is-warn`, quindi il caso positivo va senza modificatore; `Readout` accetta solo toni `up | down | flat`. Il "togli" usa `tl-row` + `tl-btn-ghost` come in `MarginCalculator`.
- **`copy.test.ts` contiene una banned-list di claim FTC.** "Profitto garantito" / "guaranteed profit" la fanno fallire. Qualsiasi etichetta o riga d'esempio che promette un esito va scritta come descrizione, non come promessa (`Profitto` + `Ritorno in ogni esito`).
- **Dove c'è una lista di input, serve anche il bottone per togliere.** Senza `removeOutcome`/`removeLeg` un campo vuoto aggiunto per sbaglio blocca il calcolatore sul trattino per sempre.
- **`ToolIcon` costruisce il path per convenzione** (`/icons/tool-<slug>.png`), non da una mappa: TypeScript non protegge, e ogni tool nuovo serve due `<img>` **404** finché non arriva la sua icona. Stato noto e accettato sul branch fino al Task 7.

### Correzioni imposte dal Task 2

- **I valori di default DEVONO essere l'esempio lavorato.** Chi apre la pagina vede subito i numeri di cui parla il testo, e il test d'integrazione monta il caso vero senza digitare. Pattern confermato due volte (arbitrage 2.10/2.10, parlay 4 × 1.80).
- **Nessuna assunzione nascosta in una costante.** Il «margine composto 21,6%» del piano non discendeva dalle gambe a 1.80: veniva da un margine del 5% per gamba mai dichiarato (dalle quote sarebbe 46,41%). Se un output dipende da un parametro che l'utente non vede, quel parametro diventa **un campo**. E l'esempio scrive ciò che il readout stampa (21,55%), non l'arrotondamento (21,6%).
- **`copy.test.ts` accetta un test per tool** che pretende i numeri dell'esempio e **rifiuta** le varianti arrotondate. Aggiungilo per ogni tool: è l'oracolo che ha pescato 4.76 contro 4.71 ad agosto.
- Registrando lo slug, il primo errore non è il mount del componente: è **`tsc` sugli 11 dizionari**, e vitest crasha nel render su `copy.labels`. Aspettati quello.

---

## File Structure

| File | Responsabilità | Azione |
|---|---|---|
| `lib/betting-math.ts` | Tutta la matematica pura, già 278 righe con 14 export | **Modify** — +5 funzioni (arbitrage, roi, yieldPercent, stakeForTarget, bankrollPlan). Parlay riusa `parlayOdds`/`parlayProbability` già presenti |
| `lib/betting-math.test.ts` | Test della matematica | **Modify** — un blocco per funzione nuova |
| `lib/tools/registry.ts` | Fonte di verità: slug, lingue, path, hreflang | **Modify** — 6 slug in `TOOL_SLUGS`. Nient'altro |
| `components/tools/ArbitrageCalculator.tsx` … `ParlayCalculator.tsx` | Un componente per tool | **Create** ×6 |
| `components/tools/ToolCalculator.tsx` | Dispatch slug → componente | **Modify** — 6 `case` |
| `components/tools/calculators.test.tsx` | Test d'integrazione dei calcolatori | **Modify** — un `describe` per tool |
| `lib/tools/copy/en.ts` + `it de es fr pt nl pl tr sv ru` | Dizionari | **Modify** ×11 — una voce per tool per lingua |
| `lib/tools/copy.test.ts` | Parità strutturale fra lingue | **Modify solo se serve** — è generico su `TOOL_SLUGS`, dovrebbe coprire i nuovi da sé. Verificarlo, non assumerlo |
| `public/tools/*.png` (o dove stanno le 5 esistenti) | Icone raster 3D | **Create** ×6 — Task 8 |

**Ordine delle fette:** Arbitrage → Parlay → ROI → Yield → Stake → Bankroll. Arbitrage per primo perché è il tool a intento più alto e il più ricco (fa da modello agli altri cinque); Parlay secondo perché la matematica esiste già.

**Nota sulla copy nel piano.** I task specificano per ogni tool: le chiavi `labels` esatte, i numeri verificati dell'esempio, il takeaway e le claim che l'explainer deve sostenere. Il testo lungo (explainer, FAQ, meta) si scrive seguendo `lib/tools/copy/en.ts` come riferimento di qualità — riprodurre 6 tool × 11 lingue di prosa dentro il piano lo renderebbe più grande dell'implementazione. **I numeri, quelli, sono nel piano e non si improvvisano.**

---

### Task 1: Arbitrage calculator

**Files:**
- Modify: `lib/betting-math.ts` (aggiungere in fondo, dopo `parlayOdds`)
- Modify: `lib/betting-math.test.ts`
- Create: `components/tools/ArbitrageCalculator.tsx`
- Modify: `components/tools/ToolCalculator.tsx`
- Modify: `lib/tools/registry.ts:7-13`
- Modify: `lib/tools/copy/en.ts` + le altre 10 lingue
- Modify: `components/tools/calculators.test.tsx`

**Interfaces:**
- Consumes: `parseOdds(input, "decimal")`, `impliedProbability(decimal)` da `lib/betting-math`; `Field`, `Readout`, `num`, `pct`, `signedPct`, `parseAmount` da `./parts`; `Meter` da `./Meter`.
- Produces: `arbitrage({ decimals, total }): { impliedSum, profitPercent, stakes, returns } | null` — consumato solo da questo tool.

- [ ] **Step 1: Write the failing test per la matematica**

In `lib/betting-math.test.ts`:

```ts
describe("arbitrage", () => {
  it("2.10 e 2.10 su due book danno +5.00% e stake simmetrici", () => {
    const r = arbitrage({ decimals: [2.1, 2.1], total: 1000 });
    expect(r).not.toBeNull();
    expect(r!.impliedSum).toBeCloseTo(0.952381, 6);
    expect(r!.profitPercent).toBeCloseTo(0.05, 6);
    expect(r!.stakes[0]).toBeCloseTo(500, 6);
    expect(r!.stakes[1]).toBeCloseTo(500, 6);
    expect(r!.returns[0]).toBeCloseTo(1050, 6);
  });

  it("senza arbitraggio il profitto è negativo, non null", () => {
    const r = arbitrage({ decimals: [1.9, 1.9], total: 1000 });
    expect(r!.impliedSum).toBeCloseTo(1.052632, 6);
    expect(r!.profitPercent).toBeLessThan(0);
  });

  it("quote asimmetriche: 3.00 e 1.60 sbilanciano gli stake ma pareggiano il ritorno", () => {
    const r = arbitrage({ decimals: [3.0, 1.6], total: 1000 });
    // 1/3 + 1/1.6 = 0.333333 + 0.625 = 0.958333
    expect(r!.impliedSum).toBeCloseTo(0.958333, 6);
    expect(r!.returns[0]).toBeCloseTo(r!.returns[1], 6);
  });

  it("input non validi tornano null, non NaN", () => {
    expect(arbitrage({ decimals: [], total: 1000 })).toBeNull();
    expect(arbitrage({ decimals: [2.1, 0], total: 1000 })).toBeNull();
    expect(arbitrage({ decimals: [2.1, 2.1], total: 0 })).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/betting-math.test.ts -t arbitrage`
Expected: FAIL — `arbitrage is not defined`

- [ ] **Step 3: Implement `arbitrage`**

In `lib/betting-math.ts`, in fondo:

```ts
/** Arbitraggio su un mercato coperto interamente da quote di book diversi.
 *  Σ(1/quota) < 1 ⇒ esiste una divisione degli stake che vince in ogni esito.
 *  Ritorna il profitto ANCHE quando è negativo (nessun arbitraggio): dire
 *  "−4,99%" è un'informazione, `null` no. `null` è riservato all'input invalido. */
export function arbitrage(args: {
  decimals: number[];
  total: number;
}): { impliedSum: number; profitPercent: number; stakes: number[]; returns: number[] } | null {
  const { decimals, total } = args;
  if (decimals.length < 2 || !Number.isFinite(total) || total <= 0) return null;
  if (decimals.some((d) => !Number.isFinite(d) || d <= 1)) return null;

  const impliedSum = decimals.reduce((s, d) => s + 1 / d, 0);
  if (!Number.isFinite(impliedSum) || impliedSum <= 0) return null;

  // Stake proporzionale alla probabilità implicita ⇒ ritorno identico su ogni esito.
  const stakes = decimals.map((d) => (total * (1 / d)) / impliedSum);
  const returns = stakes.map((s, i) => s * decimals[i]);
  const profitPercent = 1 / impliedSum - 1;

  return { impliedSum, profitPercent, stakes, returns };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/betting-math.test.ts -t arbitrage`
Expected: PASS, 4 test

- [ ] **Step 5: Commit la matematica**

```bash
git add lib/betting-math.ts lib/betting-math.test.ts
git commit -m "feat(math): funzione arbitrage — Σ(1/quota) e divisione degli stake (#TOOLS-11-0808)"
```

- [ ] **Step 6: Scrivere il componente**

Create `components/tools/ArbitrageCalculator.tsx`. Segue il pattern di `MarginCalculator` (che ha già la lista di esiti con "aggiungi"): due quote di default, bottone per il terzo esito, stake totale.

```tsx
"use client";
// components/tools/ArbitrageCalculator.tsx (#TOOLS-11-0808)
// Arbitraggio: due o più book che, sommati, lasciano un margine a chi scommette.
// Il verdetto è la prima cosa che si legge — il resto sono i numeri per eseguirlo.

import { useMemo, useState } from "react";
import { arbitrage, parseOdds } from "@/lib/betting-math";
import type { ToolCopy } from "@/lib/tools/copy";
import { Field, Readout, num, parseAmount, pct, signedPct, toneOf } from "./parts";
import { Meter } from "./Meter";

export function ArbitrageCalculator({ copy, dash }: { copy: ToolCopy; dash: string }) {
  const L = copy.labels;
  const [legs, setLegs] = useState(["2.10", "2.10"]);
  const [total, setTotal] = useState("1000");

  const result = useMemo(() => {
    const decimals = legs.map((l) => parseOdds(l, "decimal"));
    if (decimals.some((d) => d === null)) return null;
    const stake = parseAmount(total);
    if (stake === null) return null;
    return arbitrage({ decimals: decimals as number[], total: stake });
  }, [legs, total]);

  const isArb = result !== null && result.profitPercent > 0;
  const setLeg = (i: number, v: string) =>
    setLegs((prev) => prev.map((p, j) => (j === i ? v : p)));

  return (
    <div className="tl-calc tl-calc--arbitrage">
      <div className="tl-panel tl-panel--in">
        <h3 className="tl-panel-title">{L.inputTitle}</h3>
        {legs.map((leg, i) => (
          <Field
            key={i}
            label={`${L.outcome} ${i + 1}`}
            value={leg}
            onChange={(v) => setLeg(i, v)}
            placeholder="2.10"
          />
        ))}
        {legs.length < 4 ? (
          <button
            type="button"
            className="tl-add"
            onClick={() => setLegs((prev) => [...prev, ""])}
          >
            {L.addOutcome}
          </button>
        ) : null}
        <Field label={L.total} value={total} onChange={setTotal} placeholder="1000" />
        <p className="tl-hint">{L.hint}</p>
      </div>

      <div className="tl-panel tl-panel--out">
        <h3 className="tl-panel-title">{L.resultTitle}</h3>
        <Readout
          label={L.profit}
          testId="out-arb-profit"
          value={signedPct(result?.profitPercent, dash)}
          tone={toneOf(result?.profitPercent)}
          strong
        />
        <Readout label={L.impliedSum} testId="out-arb-sum" value={pct(result?.impliedSum, dash)} />
        {legs.map((_, i) => (
          <Readout
            key={i}
            label={`${L.stakeOn} ${i + 1}`}
            testId={`out-arb-stake-${i + 1}`}
            value={num(result?.stakes[i], dash)}
          />
        ))}
        <Readout label={L.guaranteedReturn} testId="out-arb-return" value={num(result?.returns[0], dash)} />
        {result ? (
          // La traccia è il 100% del mercato: il marcatore mostra quanto ne
          // coprono le quote sommate. Sotto il 100% = arbitraggio.
          <Meter
            segments={[
              { value: Math.min(result.impliedSum, 1), tone: isArb ? "edge" : "muted" },
              { value: Math.max(0, 1 - result.impliedSum), tone: "muted" },
            ]}
            markers={[{ at: 1, label: "100%" }]}
          />
        ) : null}
        <p className={`tl-verdict ${isArb ? "is-ok" : "is-warn"}`}>
          {isArb ? L.verdictArb : L.verdictNoArb}
        </p>
      </div>
    </div>
  );
}
```

⚠️ **Verificare due cose contro il codice esistente prima di dare per buono questo JSX:** (1) la classe del bottone "aggiungi" e (2) i toni ammessi da `Meter` e da `tl-verdict`. `MarginCalculator.tsx` ha già entrambi i pattern — copiare da lì i nomi reali invece di fidarsi di questo blocco. Se `is-ok` non esiste come tono di `tl-verdict`, usare quello che esiste.

- [ ] **Step 7: Registrare lo slug e il dispatch**

`lib/tools/registry.ts` — aggiungere a `TOOL_SLUGS`:

```ts
  "arbitrage-calculator",
```

`components/tools/ToolCalculator.tsx` — import + case:

```tsx
import { ArbitrageCalculator } from "./ArbitrageCalculator";
// …
    case "arbitrage-calculator":
      return <ArbitrageCalculator copy={copy} dash={dash} />;
```

- [ ] **Step 8: Verificare che TypeScript rompa (è il comportamento atteso)**

Run: `npx tsc --noEmit`
Expected: FAIL — 11 errori, uno per lingua: `Property 'arbitrage-calculator' is missing in type ... Record<ToolSlug, ToolCopy>`.
Questo conferma il vincolo dei Global Constraints. Il passo successivo lo chiude.

- [ ] **Step 9: Scrivere la copy in tutte e 11 le lingue**

In ognuno di `lib/tools/copy/{en,it,es,fr,de,pt,nl,pl,tr,sv,ru}.ts`, dentro `tools`, la voce `"arbitrage-calculator"`.

**Chiavi `labels` (identiche in ogni lingua, tradotti i valori):**
`inputTitle` · `outcome` · `addOutcome` · `total` · `resultTitle` · `profit` · `impliedSum` · `stakeOn` · `guaranteedReturn` · `verdictArb` · `verdictNoArb` · `hint`

**Esempio numerico — questi numeri sono verificati, non toccarli:**

| Riga | Valore |
|---|---|
| Quote (due book) | `2.10 · 2.10` |
| Somma delle probabilità implicite | `95.24%` |
| Stake su 1.000 | `500 · 500` |
| Ritorno in ogni esito | `1.050` |
| Profitto garantito | `+50 (+5.00%)` |

`note` dell'esempio deve dire il punto che nessun'altra pagina dice: lo stesso mercato a `1.90/1.90` somma **105.26%** e restituisce **−4.99%** — la differenza fra i due non è la fortuna, è quale book paga di più su quale lato.

**`takeaway`:** l'arbitraggio non è una previsione. Non richiede di avere ragione su chi vince: richiede due book in disaccordo abbastanza da coprire il margine di entrambi.

**L'explainer (2 paragrafi, >800 caratteri) deve sostenere queste claim:**
1. La condizione è `Σ(1/quota) < 1`, e lo stake va diviso in proporzione alle probabilità implicite — così il ritorno è identico in ogni esito e il profitto non dipende dal risultato.
2. Perché in pratica si chiude poco spesso: le quote si muovono mentre piazzi la seconda gamba, i limiti di puntata mordono, e i book limitano chi lo fa di sistema. **Va detto**, altrimenti la pagina promette una macchina da soldi — e questo è precisamente il tipo di claim che non facciamo.

**FAQ (4):** serve un conto in ogni book? · cosa succede se la seconda quota si muove? · perché i book limitano? · l'arbitraggio è legale (sì) ma i termini del book possono vietarlo?

- [ ] **Step 10: Verificare che TypeScript e la parità della copy passino**

Run: `npx tsc --noEmit && npx vitest run lib/tools/copy.test.ts`
Expected: PASS su entrambi. Se `copy.test.ts` non ha coperto il nuovo slug, **fermarsi**: significa che il test non è generico su `TOOL_SLUGS` come assunto, e va esteso prima di procedere.

- [ ] **Step 11: Scrivere il test d'integrazione del calcolatore**

In `components/tools/calculators.test.tsx`, in coda:

```tsx
describe("arbitrage calculator", () => {
  it("2.10 e 2.10 su 1000 danno +5.00% e stake 500/500", async () => {
    const user = userEvent.setup();
    mount("arbitrage-calculator");
    // Le etichette vengono dal dizionario EN: verificarle in copy/en.ts
    // se questi getByLabelText non trovano nulla.
    for (const n of [1, 2]) {
      const leg = screen.getByLabelText(`Outcome ${n}`);
      await user.clear(leg);
      await user.type(leg, "2.10");
    }
    await user.clear(screen.getByLabelText("Total stake"));
    await user.type(screen.getByLabelText("Total stake"), "1000");
    expect(screen.getByTestId("out-arb-profit").textContent).toBe("+5.00%");
    expect(screen.getByTestId("out-arb-sum").textContent).toBe("95.24%");
    expect(screen.getByTestId("out-arb-stake-1").textContent).toBe("500.00");
    expect(screen.getByTestId("out-arb-return").textContent).toBe("1050.00");
  });

  it("1.90/1.90 dice che non c'è arbitraggio invece di mostrare un profitto finto", async () => {
    const user = userEvent.setup();
    mount("arbitrage-calculator");
    for (const n of [1, 2]) {
      const leg = screen.getByLabelText(`Outcome ${n}`);
      await user.clear(leg);
      await user.type(leg, "1.90");
    }
    expect(screen.getByTestId("out-arb-profit").textContent).toBe("-4.99%");
    expect(screen.getByTestId("out-arb-sum").textContent).toBe("105.26%");
  });

  it("spazzatura in input non produce NaN", async () => {
    const user = userEvent.setup();
    mount("arbitrage-calculator");
    const leg = screen.getByLabelText("Outcome 1");
    await user.clear(leg);
    await user.type(leg, "banana");
    expect(screen.getByTestId("out-arb-profit").textContent).toBe("—");
    expect(screen.getByTestId("out-arb-stake-1").textContent).toBe("—");
  });
});
```

- [ ] **Step 12: Run test to verify it passes**

Run: `npx vitest run components/tools/calculators.test.tsx`
Expected: PASS. Il `describe("tutti i calcolatori")` che itera su `TOOL_SLUGS` copre automaticamente il nuovo slug — deve passare anche quello.

- [ ] **Step 13: Verificare la pagina reale**

Run: `npm run build 2>&1 | tail -30`
Expected: la build genera **11 pagine in più** rispetto a prima (6 tool × 11 lingue arriveranno a 132; dopo questo task siamo a 66 + 11 = **77**).
Poi: `/tools/arbitrage-calculator` e `/it/tools/arbitrage-calculator` esistono; `/tools/arbitrage` (slug sbagliato) è 404.

- [ ] **Step 14: Commit**

```bash
git add lib/tools/registry.ts components/tools/ArbitrageCalculator.tsx \
        components/tools/ToolCalculator.tsx components/tools/calculators.test.tsx \
        lib/tools/copy/
git commit -m "feat(tools): arbitrage calculator in 11 lingue (#TOOLS-11-0808)"
```

---

### Task 2: Parlay calculator

**Files:** come Task 1, con `ParlayCalculator.tsx`.

**Interfaces:**
- Consumes: `parlayOdds(decimals)` e `parlayProbability(probabilities)` — **già in `lib/betting-math.ts:267,274`**, nessuna matematica nuova. `impliedProbability` per la catena.
- Produces: nulla di condiviso.

- [ ] **Step 1: Leggere le due funzioni esistenti prima di scrivere**

Run: `sed -n '260,278p' lib/betting-math.ts`
Serve conoscere firma esatta e comportamento su input vuoto: `ProbabilityCalculator.tsx` le usa già (`out-parlay-odds`, `out-parlay-prob`) e i suoi test passano — quello è il riferimento.

- [ ] **Step 2: Scrivere il test d'integrazione (prima del componente)**

In `calculators.test.tsx`:

```tsx
describe("parlay calculator", () => {
  it("quattro gambe a 1.80 danno 10.50 e 9.53%", async () => {
    const user = userEvent.setup();
    mount("parlay-calculator");
    for (const n of [1, 2]) {
      const leg = screen.getByLabelText(`Leg ${n}`);
      await user.clear(leg);
      await user.type(leg, "1.80");
    }
    for (const n of [3, 4]) {
      await user.click(screen.getByRole("button", { name: "Add leg" }));
      const leg = screen.getByLabelText(`Leg ${n}`);
      await user.clear(leg);
      await user.type(leg, "1.80");
    }
    // 1.80^4 = 10.4976
    expect(screen.getByTestId("out-parlay-total").textContent).toBe("10.50");
    expect(screen.getByTestId("out-parlay-implied").textContent).toBe("9.53%");
  });

  it("una gamba vuota non azzera il totale con uno zero finto", async () => {
    const user = userEvent.setup();
    mount("parlay-calculator");
    const leg = screen.getByLabelText("Leg 1");
    await user.clear(leg);
    expect(screen.getByTestId("out-parlay-total").textContent).toBe("—");
  });
});
```

- [ ] **Step 3: Run test to verify it fails**

Run: `npx vitest run components/tools/calculators.test.tsx -t parlay`
Expected: FAIL — lo slug non è in `TOOL_SLUGS`, `mount` non risolve il componente.

- [ ] **Step 4: Scrivere il componente**

`components/tools/ParlayCalculator.tsx`, sul modello della sezione parlay di `ProbabilityCalculator.tsx` (leggerla e riusarne la logica delle gambe: `legs` array + bottone "add leg", massimo sensato 8). Output: quota combinata (`out-parlay-total`), probabilità implicita (`out-parlay-implied`), e il margine composto. `Meter` con la catena delle probabilità, come già fa `ProbabilityCalculator`.

- [ ] **Step 5: Registrare slug + dispatch, poi la copy in 11 lingue**

Slug: `"parlay-calculator"`. Labels: `inputTitle` · `leg` · `addLeg` · `removeLeg` · `resultTitle` · `combinedOdds` · `impliedProb` · `compoundMargin` · `hint` · `verdict`.

**Esempio numerico verificato:**

| Riga | Valore |
|---|---|
| Gambe | `4 × 1.80` |
| Quota combinata | `10.4976` |
| Probabilità implicita | `9.53%` |
| Margine composto | `21.6%` |

**`takeaway`:** il margine del book non si somma sulle gambe, si **compone** — quattro gambe da 1.80 sembrano quattro scommesse quasi equilibrate e sono un evento al 9,53%.

**Explainer:** (1) la quota combinata è il prodotto, la probabilità il prodotto delle probabilità — quindi ogni gamba aggiunta moltiplica anche il margine; (2) perché le multiple sono il prodotto più venduto e il meno favorevole, e il legame con la correlazione fra gambe (due gambe correlate non moltiplicano in modo indipendente).

- [ ] **Step 6: Run test to verify it passes**

Run: `npx tsc --noEmit && npx vitest run components/tools/calculators.test.tsx lib/tools/copy.test.ts`
Expected: PASS

- [ ] **Step 7: Verificare che il vecchio tool non sia regredito**

Run: `npx vitest run components/tools/calculators.test.tsx -t "probability calculator"`
Expected: PASS — i test della catena su `probability-calculator` (`out-parlay-odds`, `out-parlay-prob`) **devono continuare a passare**: il tool nuovo aggiunge una rotta, non sposta la matematica esistente.

- [ ] **Step 8: Commit**

```bash
git add lib/tools/registry.ts components/tools/ParlayCalculator.tsx \
        components/tools/ToolCalculator.tsx components/tools/calculators.test.tsx \
        lib/tools/copy/
git commit -m "feat(tools): parlay calculator con rotta propria (#TOOLS-11-0808)"
```

---

### Task 3: ROI calculator

**Files:** come Task 1, con `RoiCalculator.tsx`.

**Interfaces:**
- Produces: `roi({ profit, capital }): number | null` — frazione, non percentuale (la formattazione è di `pct`).

- [ ] **Step 1: Write the failing test**

```ts
describe("roi", () => {
  it("400 di profitto su 1000 di capitale è il 40%", () => {
    expect(roi({ profit: 400, capital: 1000 })).toBeCloseTo(0.4, 9);
  });
  it("una perdita dà un ROI negativo", () => {
    expect(roi({ profit: -250, capital: 1000 })).toBeCloseTo(-0.25, 9);
  });
  it("capitale zero o negativo è null, non Infinity", () => {
    expect(roi({ profit: 400, capital: 0 })).toBeNull();
    expect(roi({ profit: 400, capital: -10 })).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/betting-math.test.ts -t roi`
Expected: FAIL — `roi is not defined`

- [ ] **Step 3: Implement**

```ts
/** ROI: profitto sul CAPITALE impiegato (la cassa), non sul giocato.
 *  La differenza con lo yield è il punto delle due pagine — vedi yieldPercent. */
export function roi(args: { profit: number; capital: number }): number | null {
  const { profit, capital } = args;
  if (!Number.isFinite(profit) || !Number.isFinite(capital) || capital <= 0) return null;
  return profit / capital;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/betting-math.test.ts -t roi`
Expected: PASS, 3 test

- [ ] **Step 5: Componente, slug, dispatch, copy**

`RoiCalculator.tsx`: input `capital` e `profit` (oppure ritorno totale — scegliere UNA modalità: capitale + profitto, così non serve un segmented). Output `out-roi` con `signedPct`, tono da `toneOf`. `Meter`: il ROI contro lo zero.

Labels: `inputTitle` · `capital` · `profit` · `resultTitle` · `roi` · `hint` · `verdict`.

**Esempio verificato:** capitale `1.000`, profitto `400` → **40.00%**.

**`takeaway`:** il ROI dice quanto ha reso la **cassa**; da solo non dice se la strategia è buona, perché la stessa cassa può produrlo con 20 scommesse o con 2.000.

**Explainer:** (1) definizione e perché il denominatore va scelto e dichiarato; (2) **il collegamento allo Yield con link alla sua pagina** — è il paragrafo che fa lavorare le due pagine insieme.

- [ ] **Step 6: Test d'integrazione + verifica**

```tsx
describe("roi calculator", () => {
  it("1000 di capitale e 400 di profitto danno +40.00%", async () => {
    const user = userEvent.setup();
    mount("roi-calculator");
    await user.clear(screen.getByLabelText("Capital"));
    await user.type(screen.getByLabelText("Capital"), "1000");
    await user.clear(screen.getByLabelText("Profit"));
    await user.type(screen.getByLabelText("Profit"), "400");
    expect(screen.getByTestId("out-roi").textContent).toBe("+40.00%");
  });
});
```

Run: `npx tsc --noEmit && npx vitest run components/tools/calculators.test.tsx lib/tools/copy.test.ts`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add lib/betting-math.ts lib/betting-math.test.ts lib/tools/registry.ts \
        components/tools/RoiCalculator.tsx components/tools/ToolCalculator.tsx \
        components/tools/calculators.test.tsx lib/tools/copy/
git commit -m "feat(tools): ROI calculator in 11 lingue (#TOOLS-11-0808)"
```

---

### Task 4: Yield calculator

**Files:** come Task 1, con `YieldCalculator.tsx`.

**Interfaces:**
- Produces: `yieldPercent({ profit, turnover }): number | null` — frazione. Nome `yieldPercent` e **non** `yield`: `yield` è una parola riservata in JS strict/generator.

- [ ] **Step 1: Write the failing test**

```ts
describe("yieldPercent", () => {
  it("400 di profitto su 10.000 giocati è il 4%", () => {
    expect(yieldPercent({ profit: 400, turnover: 10000 })).toBeCloseTo(0.04, 9);
  });
  it("lo stesso profitto su un turnover diverso dà uno yield diverso", () => {
    // È il punto delle due pagine: stesso 400, cassa 1000 → ROI 40%, giocato 10k → yield 4%
    expect(yieldPercent({ profit: 400, turnover: 1000 })).toBeCloseTo(0.4, 9);
  });
  it("turnover zero è null, non Infinity", () => {
    expect(yieldPercent({ profit: 400, turnover: 0 })).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/betting-math.test.ts -t yieldPercent`
Expected: FAIL

- [ ] **Step 3: Implement**

```ts
/** Yield: profitto sul TOTALE GIOCATO (turnover). È la metrica con cui si
 *  confrontano scommettitori diversi, perché non dipende da quanta cassa hanno. */
export function yieldPercent(args: { profit: number; turnover: number }): number | null {
  const { profit, turnover } = args;
  if (!Number.isFinite(profit) || !Number.isFinite(turnover) || turnover <= 0) return null;
  return profit / turnover;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/betting-math.test.ts -t yieldPercent`
Expected: PASS, 3 test

- [ ] **Step 5: Componente, slug, dispatch, copy**

`YieldCalculator.tsx`: input numero di scommesse + stake medio (→ turnover calcolato e mostrato) e profitto. Mostrare il turnover derivato come readout `out-turnover`: è l'input che la gente sbaglia.

Labels: `inputTitle` · `bets` · `avgStake` · `profit` · `resultTitle` · `turnover` · `yield` · `hint` · `verdict`.

**Esempio verificato:** `200` scommesse × `50` = **10.000** di turnover, profitto `400` → **4.00%**.

**`takeaway`:** lo yield è la metrica che si confronta fra scommettitori diversi — un 4% su 10.000 giocati vale più di un 40% di ROI ottenuto su venti scommesse.

**Explainer:** (1) yield vs ROI, con **link reciproco alla pagina ROI** e i numeri del caso identico (stesso 400 di profitto, due denominatori); (2) perché uno yield sopra ~5% sostenuto su grandi volumi è raro, e perché su meno di qualche centinaio di scommesse il numero è rumore. Questo secondo punto è quello che ci tiene onesti.

- [ ] **Step 6: Test d'integrazione + verifica**

```tsx
describe("yield calculator", () => {
  it("200 scommesse da 50 con 400 di profitto danno +4.00%", async () => {
    const user = userEvent.setup();
    mount("yield-calculator");
    await user.clear(screen.getByLabelText("Number of bets"));
    await user.type(screen.getByLabelText("Number of bets"), "200");
    await user.clear(screen.getByLabelText("Average stake"));
    await user.type(screen.getByLabelText("Average stake"), "50");
    await user.clear(screen.getByLabelText("Profit"));
    await user.type(screen.getByLabelText("Profit"), "400");
    expect(screen.getByTestId("out-turnover").textContent).toBe("10000.00");
    expect(screen.getByTestId("out-yield").textContent).toBe("+4.00%");
  });
});
```

Run: `npx tsc --noEmit && npx vitest run components/tools/calculators.test.tsx lib/tools/copy.test.ts`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add lib/betting-math.ts lib/betting-math.test.ts lib/tools/registry.ts \
        components/tools/YieldCalculator.tsx components/tools/ToolCalculator.tsx \
        components/tools/calculators.test.tsx lib/tools/copy/
git commit -m "feat(tools): yield calculator in 11 lingue (#TOOLS-11-0808)"
```

---

### Task 5: Stake calculator

**Files:** come Task 1, con `StakeCalculator.tsx`.

**Interfaces:**
- Produces: `stakeForTarget({ targetProfit, decimal }): number | null`

- [ ] **Step 1: Write the failing test**

```ts
describe("stakeForTarget", () => {
  it("100 di profitto a quota 2.50 chiede 66.67 di stake", () => {
    expect(stakeForTarget({ targetProfit: 100, decimal: 2.5 })).toBeCloseTo(66.666667, 6);
  });
  it("a quota 2.00 lo stake è pari al profitto", () => {
    expect(stakeForTarget({ targetProfit: 100, decimal: 2 })).toBeCloseTo(100, 9);
  });
  it("quota 1.00 o minore è null (nessun profitto possibile), non Infinity", () => {
    expect(stakeForTarget({ targetProfit: 100, decimal: 1 })).toBeNull();
    expect(stakeForTarget({ targetProfit: 100, decimal: 0.5 })).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/betting-math.test.ts -t stakeForTarget`
Expected: FAIL

- [ ] **Step 3: Implement**

```ts
/** Lo stake che produce un profitto obiettivo a una data quota.
 *  Il profitto netto è stake × (quota − 1) ⇒ stake = obiettivo / (quota − 1). */
export function stakeForTarget(args: { targetProfit: number; decimal: number }): number | null {
  const { targetProfit, decimal } = args;
  if (!Number.isFinite(targetProfit) || targetProfit <= 0) return null;
  if (!Number.isFinite(decimal) || decimal <= 1) return null;
  return targetProfit / (decimal - 1);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/betting-math.test.ts -t stakeForTarget`
Expected: PASS, 3 test

- [ ] **Step 5: Componente, slug, dispatch, copy**

`StakeCalculator.tsx`: input quota + profitto obiettivo. Output stake richiesto (`out-stake-needed`), ritorno totale (`out-total-return` = stake + profitto), e **la quota di una cassa dichiarata che quello stake impegna** (`out-bankroll-share`) con un campo bankroll opzionale — è l'aggancio che manda alla pagina Kelly.

Labels: `inputTitle` · `odds` · `targetProfit` · `bankroll` · `resultTitle` · `stakeNeeded` · `totalReturn` · `bankrollShare` · `hint` · `verdict`.

**Esempio verificato:** profitto `100` a quota `2.50` → stake **66.67**, ritorno totale **166.67**; su una cassa da `1.000` è il **6.67%**.

**`takeaway`:** partire dal profitto che vuoi è il modo più rapido di puntare troppo: la domanda giusta non è quanto voglio vincere, è quanto posso permettermi di perdere.

**Explainer:** (1) la formula e la trappola del ragionamento a obiettivo (inseguire una perdita porta a stake crescenti su quote decrescenti); (2) **rimando esplicito a Kelly**: qui decidi lo stake da un desiderio, là da un edge. Il link è la parte utile.

- [ ] **Step 6: Test d'integrazione + verifica**

```tsx
describe("stake calculator", () => {
  it("100 di profitto a 2.50 chiede 66.67", async () => {
    const user = userEvent.setup();
    mount("stake-calculator");
    await user.clear(screen.getByLabelText("Odds"));
    await user.type(screen.getByLabelText("Odds"), "2.50");
    await user.clear(screen.getByLabelText("Target profit"));
    await user.type(screen.getByLabelText("Target profit"), "100");
    expect(screen.getByTestId("out-stake-needed").textContent).toBe("66.67");
    expect(screen.getByTestId("out-total-return").textContent).toBe("166.67");
  });
});
```

Run: `npx tsc --noEmit && npx vitest run components/tools/calculators.test.tsx lib/tools/copy.test.ts`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add lib/betting-math.ts lib/betting-math.test.ts lib/tools/registry.ts \
        components/tools/StakeCalculator.tsx components/tools/ToolCalculator.tsx \
        components/tools/calculators.test.tsx lib/tools/copy/
git commit -m "feat(tools): stake calculator in 11 lingue (#TOOLS-11-0808)"
```

---

### Task 6: Bankroll calculator

**Files:** come Task 1, con `BankrollCalculator.tsx`.

**Interfaces:**
- Produces: `bankrollPlan({ bankroll, unitPercent, losingStreak }): { unit, streakLoss, streakDrawdown, betsToRuin } | null`

- [ ] **Step 1: Write the failing test**

```ts
describe("bankrollPlan", () => {
  it("2000 al 2% dà unità 40, dieci perse = 400 e il 20% di drawdown, 50 giocate a rovina", () => {
    const r = bankrollPlan({ bankroll: 2000, unitPercent: 0.02, losingStreak: 10 });
    expect(r).not.toBeNull();
    expect(r!.unit).toBeCloseTo(40, 9);
    expect(r!.streakLoss).toBeCloseTo(400, 9);
    expect(r!.streakDrawdown).toBeCloseTo(0.2, 9);
    expect(r!.betsToRuin).toBe(50);
  });

  it("betsToRuin è intero e arrotonda per difetto", () => {
    // 1000 al 3% → unità 30 → 33.33 giocate → 33, non 34: la 34ª non è coperta.
    const r = bankrollPlan({ bankroll: 1000, unitPercent: 0.03, losingStreak: 5 });
    expect(r!.betsToRuin).toBe(33);
  });

  it("una percentuale fuori da 0..1 è null", () => {
    expect(bankrollPlan({ bankroll: 2000, unitPercent: 0, losingStreak: 10 })).toBeNull();
    expect(bankrollPlan({ bankroll: 2000, unitPercent: 1.5, losingStreak: 10 })).toBeNull();
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run lib/betting-math.test.ts -t bankrollPlan`
Expected: FAIL

- [ ] **Step 3: Implement**

```ts
/** Gestione della cassa a stake piatto: dimensione dell'unità, quanto costa una
 *  serie negativa, e quante giocate perse di fila azzerano la cassa.
 *  `betsToRuin` è floor: la giocata che la cassa non copre più non si fa. */
export function bankrollPlan(args: {
  bankroll: number;
  unitPercent: number;
  losingStreak: number;
}): { unit: number; streakLoss: number; streakDrawdown: number; betsToRuin: number } | null {
  const { bankroll, unitPercent, losingStreak } = args;
  if (!Number.isFinite(bankroll) || bankroll <= 0) return null;
  if (!Number.isFinite(unitPercent) || unitPercent <= 0 || unitPercent > 1) return null;
  if (!Number.isFinite(losingStreak) || losingStreak < 0) return null;

  const unit = bankroll * unitPercent;
  const streakLoss = unit * losingStreak;
  return {
    unit,
    streakLoss,
    streakDrawdown: streakLoss / bankroll,
    betsToRuin: Math.floor(bankroll / unit),
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run lib/betting-math.test.ts -t bankrollPlan`
Expected: PASS, 3 test

- [ ] **Step 5: Componente, slug, dispatch, copy**

`BankrollCalculator.tsx`: input cassa, percentuale per unità, lunghezza della serie negativa. Output unità (`out-unit`), perdita della serie (`out-streak-loss`), drawdown (`out-drawdown`), giocate a rovina (`out-bets-to-ruin`). `Meter`: il drawdown della serie contro la cassa intera — il numero che fa capire il rischio meglio di qualsiasi frase.

Labels: `inputTitle` · `bankroll` · `unitPercent` · `losingStreak` · `resultTitle` · `unit` · `streakLoss` · `drawdown` · `betsToRuin` · `hint` · `verdict`.

**Esempio verificato:** cassa `2.000` al `2%` → unità **40**; dieci perse di fila = **400**, cioè il **20%** della cassa; a stake piatto la cassa copre **50** giocate perse consecutive.

**`takeaway`:** la percentuale per unità non è una preferenza, è la scelta di quanto lunga può essere la tua serie negativa peggiore prima di uscire dal gioco.

**Explainer:** (1) stake piatto in percentuale e cosa lo distingue dallo stake fisso, con la simmetria rotta del recupero (−50% richiede +100%); (2) perché una serie di 10 perse a quote intorno a 2.00 è **normale e non sfortuna** — e quindi perché unità sopra il 5% sono una scommessa sul non incontrarla. Rimando a Kelly per quando l'edge è misurato.

- [ ] **Step 6: Test d'integrazione + verifica**

```tsx
describe("bankroll calculator", () => {
  it("2000 al 2% dà unità 40 e 20% di drawdown su dieci perse", async () => {
    const user = userEvent.setup();
    mount("bankroll-calculator");
    await user.clear(screen.getByLabelText("Bankroll"));
    await user.type(screen.getByLabelText("Bankroll"), "2000");
    await user.clear(screen.getByLabelText("Unit size (%)"));
    await user.type(screen.getByLabelText("Unit size (%)"), "2");
    await user.clear(screen.getByLabelText("Losing streak"));
    await user.type(screen.getByLabelText("Losing streak"), "10");
    expect(screen.getByTestId("out-unit").textContent).toBe("40.00");
    expect(screen.getByTestId("out-drawdown").textContent).toBe("20.00%");
    expect(screen.getByTestId("out-bets-to-ruin").textContent).toBe("50.00");
  });
});
```

⚠️ `betsToRuin` è un intero: se il readout usa `num(...)` mostra `50.00`. Decidere e **allineare test e componente** — se si vuole `50`, serve `num(v, dash, 0)`. Verificare quale forma è coerente con gli altri tool prima di fissare l'assert.

Run: `npx tsc --noEmit && npx vitest run components/tools/calculators.test.tsx lib/tools/copy.test.ts`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add lib/betting-math.ts lib/betting-math.test.ts lib/tools/registry.ts \
        components/tools/BankrollCalculator.tsx components/tools/ToolCalculator.tsx \
        components/tools/calculators.test.tsx lib/tools/copy/
git commit -m "feat(tools): bankroll calculator in 11 lingue (#TOOLS-11-0808)"
```

---

### Task 6b: La copy dell'hub dice ancora "cinque"

Scoperto nel Task 1 e **non previsto dal piano originale**: l'hub si descrive per numero. Con 11 tool si contraddice da solo su ogni lingua.

**Files:**
- Modify: `lib/tools/copy/{en,it,es,fr,de,pt,nl,pl,tr,sv,ru}.ts` — blocco `hub`

- [ ] **Step 1: Trovare ogni occorrenza del numero**

Run: `grep -rn "Five\|cinque\|Cinco\|Cinq\|Fünf\|Vijf\|Pięć\|Beş\|Fem\|Пять\|five" lib/tools/copy/`
Cerca in `hub.metaTitle`, `hub.metaDescription`, `hub.h1`, `hub.lede`, `hub.intro[]`. Il numero può comparire in cifra o in parola.

- [ ] **Step 2: Decidere UNA volta come si conta**

Undici tool. Scrivere **"11"** in cifra nei meta (conta nei 60 caratteri del title) e in parola nel corpo dove suona meglio nella lingua. Se un `metaTitle` sfonda i ~60 caratteri con "11", togliere il numero invece di troncare la frase.

- [ ] **Step 3: Applicare in 11 lingue e verificare**

Run: `npx vitest run lib/tools/copy.test.ts lib/tools/seo.test.ts && npx tsc --noEmit`
Expected: verde. Poi ri-grep: **zero** occorrenze del vecchio numero.

- [ ] **Step 4: Commit**

```bash
git add lib/tools/copy/
git commit -m "fix(tools): l'hub non si descrive più come 'cinque calcolatori' (#TOOLS-11-0808)"
```

---

### Task 7: Le sei icone

**Files:**
- Create: 6 icone nella cartella e nel formato delle 5 esistenti (individuarla: `grep -rn "ToolIcon" components/tools/ToolIcon.tsx` e seguire il path)
- Modify: `components/tools/ToolIcon.tsx` se la mappa slug→asset è lì

- [ ] **Step 1: Leggere come sono referenziate le 5 esistenti**

Run: `cat components/tools/ToolIcon.tsx`
Serve il meccanismo reale (mappa statica? path per convenzione dallo slug?) prima di generare qualsiasi immagine.

- [ ] **Step 2: Generare le sei icone**

Raster 3D nella famiglia esistente via **gptimg**, poi scontorno flood-fill dal bordo. **Mai line-art** (regola standing).
**Protocollo anti-blocco obbligatorio** (gptimg si impianta dopo la prima generazione): `pkill` di `codex exec` fra una generazione e l'altra, finestra in primo piano, prompt corti. Una icona per volta, verificando il file prodotto.

Soggetti, coerenti coi cinque esistenti: arbitraggio = due quote a confronto / bilancia; parlay = catena di gambe; ROI = capitale che cresce; yield = flusso su volume; stake = fiche singola misurata; bankroll = pila di fiche con una porzione staccata.

- [ ] **Step 3: Verificare visivamente le sei icone**

Aprire `/tools` e ogni pagina nuova: l'icona compare nell'header pagina, nella card dell'hub e nella chip. Nessun bordo bianco residuo dallo scontorno, dimensioni coerenti con le esistenti.

- [ ] **Step 4: Commit**

```bash
git add components/tools/ToolIcon.tsx public/
git commit -m "feat(tools): sei icone 3D per i tool nuovi (#TOOLS-11-0808)"
```

---

### Task 8: Verifica finale e sitemap

**Files:**
- Modify: il generatore di sitemap **solo se** non deriva da `TOOL_SLUGS`
- Modify: eventuale test Playwright dei tool

- [ ] **Step 1: Verificare che la sitemap sia derivata, non elencata a mano**

Run: `grep -rn "tools" app/sitemap.ts app/sitemap.xml* 2>/dev/null | head`
Se le URL dei tool sono elencate a mano, **aggiungerle è obbligatorio**: il criterio di successo è l'indicizzazione, e una pagina fuori sitemap non esiste. Se derivano da `TOOL_SLUGS`, non toccare nulla e passare allo step 2.

- [ ] **Step 2: Conteggio delle URL nella sitemap**

Run: `npm run build && grep -c "/tools" .next/server/app/sitemap.xml.body 2>/dev/null || npx next start & sleep 5 && curl -s localhost:3000/sitemap.xml | grep -c "/tools"`
Expected: **132** URL sotto `/tools` (121 pagine tool + 11 hub).
Se il percorso dell'artefatto è diverso, trovarlo — il numero da vedere è 132.

- [ ] **Step 3: Verificare le 404 e i canonical**

Expected: `/tools/xxx` → 404 · `/zz/tools` → 404 · `/en/tools` → 404 (l'inglese vive su `/tools`).
Su ogni pagina nuova: canonical assoluto corretto, **12 hreflang**, JSON-LD FAQPage nell'HTML servito.

- [ ] **Step 4: Playwright a 1440 e 390px sui sei tool**

Expected: nessun overflow orizzontale **del documento** (la trappola `.portal-root`: `document.scrollWidth <= viewport`), il calcolatore visibile e usabile, le chip lingua che scorrono in orizzontale su mobile.

- [ ] **Step 5: Test dell'aritmetica delle stringhe in 11 lingue**

I test che confrontano i numeri **scritti nella copy** con quelli che il calcolatore produce (esistono già per i 5 tool: trovarli con `grep -rn "aritmetica\|arithmetic" lib/tools/copy.test.ts`) vanno estesi ai sei nuovi, in tutte e 11 le lingue.
Expected: PASS. Questo è il test che ad agosto ha pescato «1.91 → 52.38% → 4.76 punti» contro un calcolatore che diceva 4.71%.

- [ ] **Step 6: Suite completa**

Run: `npx vitest run && npx tsc --noEmit`
Expected: verde. La suite era a 960 test prima di questo lavoro: il numero deve essere salito, non sceso.

- [ ] **Step 7: Commit e PR**

```bash
git add -A
git commit -m "test(tools): verifica 132 URL, hreflang e aritmetica della copy (#TOOLS-11-0808)"
git push -u origin betredge/tools-11-referral-v2
gh pr create --title "feat(tools): hub /tools da 5 a 11 calcolatori (#TOOLS-11-0808)" --body "..."
```

⚠️ **Fase A si ferma alla PR.** Il deploy in produzione è una decisione di Andrea, e la Fase B (referral) non è in questo PR.

---

## Self-Review

**1. Spec coverage.** Ogni requisito della Parte 2 della spec ha un task: 6 slug in registry (T1-T6) · esempi numerici verificati (T1-T6, tabelle) · `Meter` con un numero vero (T1-T6) · icone 3D (T7) · nessun blocco formula (Global Constraints) · trappole `.portal-role`/`SiteTopbar`/caratteri (Global Constraints + T8) · sitemap (T8) · i 7 criteri di verifica della spec mappano su T8 step 1-6.
Non coperto deliberatamente: la revisione madrelingua delle traduzioni (fuori scope dichiarato nella spec).

**2. Placeholder scan.** Il testo lungo della copy (explainer, FAQ, meta) non è riprodotto parola per parola: è una scelta dichiarata nel File Structure, con le claim obbligatorie e **tutti i numeri** specificati per ogni tool. I `--body "..."` della PR e i soggetti delle icone sono le sole altre libertà, entrambe editoriali.

**3. Type consistency.** `arbitrage` ritorna `{impliedSum, profitPercent, stakes, returns}` — usato con questi nomi nel componente di T1. `roi`/`yieldPercent` ritornano `number | null` (frazione, formattata da `pct`/`signedPct`). `stakeForTarget` → `number | null`. `bankrollPlan` → `{unit, streakLoss, streakDrawdown, betsToRuin}`, usato con questi nomi in T6. `yieldPercent` e non `yield` (parola riservata). I `testId` sono unici per tool e coerenti fra componente e test.

**Due punti dove il piano dice esplicitamente «verifica, non fidarti»**, perché sono i due dove ho inferito dal pattern senza leggere il file: le classi CSS del bottone "aggiungi" e i toni di `tl-verdict`/`Meter` (T1 step 6), e la formattazione intera di `betsToRuin` (T6 step 6).
